-- ============================================================
-- Migration: เก็บประเภทรถที่เลือกไว้สำหรับแผนภาพจุดเสียหาย
-- (sedan / estima / suv) — ไม่งั้นเปิดดูใหม่จะไม่รู้ว่าต้องโชว์ชุดรูปไหน
-- ============================================================

alter table jobs add column if not exists car_diagram_type text not null default 'sedan';
