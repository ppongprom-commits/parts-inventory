-- ============================================================
-- เพิ่ม "วันที่คาดว่าจะเสร็จ" ต่องาน (jobs) — เติมอัตโนมัติจาก
-- job_type_bundle_templates.estimated_duration_days ตอนเลือก/สร้างเซตในหน้างาน
-- แก้ไขเองภายหลังได้เสมอ (ไม่ล็อกตามเซต) ใช้เรียงลำดับ "รถที่กำลังซ่อม" ใน Owner Dashboard v0.2
-- ============================================================

alter table jobs
  add column if not exists estimated_completion_date date;
